{% extends 'base.html.twig' %}

{% block title %}Commande index{% endblock %}

{% block body %}
    <h1>Commande index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Datecom</th>
                <th>Etat</th>
                {# <th>commande</th>
					<th>image</th>
					<th>quantité</th> #}
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commande in commandes %}
            <tr>
                <td>{{ commande.id }}</td>
                <td>{{ commande.datecom ? commande.datecom|date('Y-m-d') : '' }}</td>
                <td>{{ commande.etat }}</td>
                <td>
                {# {% for cp in commande.commandeProduits %}
								{{(cp.produit.nom) }}</td>
						</td>
						<td>
							<img src="{{cp.produit.image}}" alt="" class="imgreg img-fluid thumbnail">
						</td>

						<td>{{(cp.quantite)}}</td>
                        
					{% endfor %} #}
                    <a href="{{ path('app_commande_show', {'id': commande.id}) }}">detail</a>
                    <a href="{{ path('app_commande_edit', {'id': commande.id}) }}">modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_commande_new') }}">Create new</a>
{% endblock %}
