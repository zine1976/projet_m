{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>Mon compte</h1>

    <table class="table">
        <tbody>
            {% if is_granted('ROLE_ADMIN') %}

            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
				{% endif %}
            <tr>
                <th>Nom</th>
                <td>{{ user.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user.prenom }}</td>
            </tr>
           
           
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
        </tbody>
    </table>
    
   


     <div class="list-group_user">
  <a href="{{ path('app_user_edit', {'id': user.id}) }}" class="list-group-item list-group-item-action">Modifier des données</a>

  <a href="{{path('app_commande_index', {id: app.user.id})}}" class="list-group-item list-group-item-action" aria-current="true">
    Mes commandes
  </a>
  <a href="{{path('app_adresse_index')}}" class="list-group-item list-group-item-action">Mon adresse de livraison</a>
  <a href="{{path('app_adressefact_index')}}" class="list-group-item list-group-item-action">Mon adresse de facturation</a>
  
  <a href="{{path('app_donnees_pdf', {id: user.id})}}" class="list-group-item list-group-item-action">exporter mes données<img src="{{asset('pdfico.ico') }}"></a>
</div>
    <div class="compteperso">

<a class="btn btn-danger"{{ include('user/_delete_form.html.twig') }}</a>
</div>
				{% if is_granted('ROLE_ADMIN') %}

    <a href="{{ path('app_user_index') }}">liste</a>


				{% endif %}

{% endblock %}
