{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>{{ categorie.nom|capitalize }}</h1>

    <table class="table">
        <tbody>
            {% for produit in produits %}
                
                <div class="pouf">

		<div class="pouf1">
			{# <a href="{{path('app_detailproduit',{id:26})}}"> #}
				<img src="{{ asset("uploads/" ~ produit.image) }}" class="card-img-top" alt="..."></a>
				<p class="surplus5">{{ produit.nom }}</p>
		
		</div>
            {% endfor %}

        </div>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_index') }}">back to list</a>

    <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}">edit</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
