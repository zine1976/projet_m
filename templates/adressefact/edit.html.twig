{% extends 'base.html.twig' %}

{% block title %}modifier Adresse de facturation{% endblock %}

{% block body %}
    <h1>Modifier votre adresse de facturation</h1>

    {{ include('adressefact/_form.html.twig', {'button_label': 'Update'}) }}
				{% if is_granted('ROLE_ADMIN') %}

    <a href="{{ path('app_adressefact_index') }}">back to list</a>
				{% endif %}

    {{ include('adressefact/_delete_form.html.twig') }}
{% endblock %}
