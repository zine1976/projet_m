<form method="post" action="{{ path('app_adressefact_delete', {'id': adressefact.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adressefact.id) }}">
    <button class="btn">Delete</button>
</form>
