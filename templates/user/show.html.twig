{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            {% if is_granted('ROLE_ADMIN') %}

            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
				{% endif %}
            <tr>
                <th>Nom</th>
                <td>{{ user.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user.prenom }}</td>
            </tr>
           
           
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
        </tbody>
    </table>
    <a href="{{ path('app_user_edit', {'id': user.id}) }}">modifier mes données</a>

				{# {% if is_granted('ROLE_ADMIN') %} #}

    <a href="{{ path('app_user_index') }}">liste</a>


    <a class="btn btn-danger"{{ include('user/_delete_form.html.twig') }}</a>
				{# {% endif %} #}

{% endblock %}
