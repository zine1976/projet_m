{% extends 'base.html.twig' %}

{% block title %}liste
{% endblock %}

{% block body %}


	<div class="ajoutprodtitre">
		<h1>liste des utilisateurs</h1>
	</div>
	<table class="table">
		<thead>
			<tr>
				<th>Nom</th>
				<th>prenom</th>
				<th>email</th>
			</tr>
		</thead>
		<tbody>
			{% for util in utilisateur %}
				<tr>
					<td>{{ util.nom }}</td>
					<td>{{ util.prenom }}</td>
					<td>{{ util.email }}</td>
					


						<td>
														<a class="btn btn-danger" href="{{path('app_delete',{id: util.id})}}">Supprimer un utilisateur</a>
													</td>
					</td>
				</tr>

				{% else %} 
				 <tr>
									<td colspan="5">aucun utilisateur trouvé</td>
								</tr>
			{% endfor %}
		</tbody>
	</table>

{% endblock %}